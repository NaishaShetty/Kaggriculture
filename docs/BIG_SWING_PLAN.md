> **Update, post-Phase 53 — the simplest 4th-quadrant idea (same crops, same execution, just scaled targets) also fails, but for a genuinely new and precisely diagnosed reason: cash-flow/ROI timing, not the worker-turn capacity ceiling or dedicated-hands recurring-cost mechanism every prior attempt hit.** Following directly from Phase 52's finding that 2 of our 3 recent real losses were opponents running our own exact MELON/STRAWBERRY/WHEAT portfolio at a 4th land quadrant, this phase built an isolated wrapper (`scripts/phase53/fourth_quadrant_portfolio.py`, composed with Submission K's own unmodified `scripts/phase46/feed_priority_execution.py` + `scripts/phase48/capped_animal_portfolio.py`, `agents/phase21/portfolio.py` untouched) extending the existing land/hands/crop-tile rungs with a 4th tier, grounded in a corrected direct re-trace of the real yuki/Sarthak Patel replays (land=4 actually reached day 12/17, not day 9-11 as initially framed) and Phase 30/46's own idle-fraction data. **The mandatory 4-seed isolated screen is a decisive, consistent regression (-6.1% mean, 4/4 seeds worse)**, correctly stopping before any 15-seed validation per this project's own screen-first precedent. **A direct trace (seed 700000) found idle-fraction actually IMPROVED (0.107→0.091) and tile-servicing (WATER/HARVEST throughput) scaled up correctly** — ruling out both the worker-turn capacity ceiling Phase 30/46 characterized AND Phase 33/48's dedicated-hands mechanism (this variant uses the plain shared hand pool, no new crop). **The real, precisely-quantified mechanism**: `agents/phase21/execution.py`'s HIRE section recomputes its Fibonacci cost schedule from `fib(0)` every day because hired hands reset to empty at each day's start (confirmed directly in the trace) — so raising the hand-count TARGET at all (dedicated or not) creates a recurring DAILY cost, not a one-time one: +2 hands (11→13) costs an extra $377/day, $5,278 over the remaining 14 days, on top of the 4th quadrant's one-time $4,000 land price (the steepest of the game) — a combined cost arriving too late in the fixed 30-day season to pay back against the extra tiles' harvest revenue. This generalizes Phase 33/48's dedicated-hands diagnosis: the recurring-cost problem isn't specific to a separate dedicated batch, it's inherent to the HIRE mechanic's daily reset for ANY hand-count increase. **Not ready to package** — Submission K remains the correct, unaffected upload candidate. The underlying idea (same crops, more land) isn't disproven, only this specific rung timing; a future attempt would need either meaningfully earlier 4th-quadrant timing (more runway to amortize the cost) or a way to add land-servicing capacity without raising the shared hand-count target at all. Full detail in `results/phase53/PHASE53_OWN_PORTFOLIO_4TH_QUADRANT_REPORT.md`.
>
> **Update, post-Phase 52 — the 3 most recent real losses trace to two already-identified levers, not a new pattern, and our own side plays identically in losses and wins.** A live leaderboard check found our 10 most recent real games at 7W-3L, a marked improvement over Phase 41's earlier 4W-5L snapshot; per this project's standing discipline of looking at real losses directly, the 3 losses (105750065 vs. Shawon Biswas, 105745378 vs. yuki, 105744439 vs. Sarthak Patel) were fetched via the same public no-login replay endpoint used since Phase 19 and forensically analyzed with `scripts/phase52/loss_forensics.py`, a thin wrapper (no frozen module touched) that runs Phase 41's own unmodified `team_features` vocabulary on BOTH sides of `agents/phase6/replay_forensics.py::extract_episode_timelines` for the first time — previously only ever applied to the opponent side. **2 of 3 losses (yuki, Sarthak Patel) are opponents running our own exact MELON/STRAWBERRY/WHEAT crop portfolio at 4 land quadrants instead of our capped 3** — a fresh, real instance of Phase 41's flagged, never-validated "whole-portfolio 4th-quadrant scale-up" archetype; **the third (Shawon Biswas) is animal-heavy (18 animals vs. our 7)**, matching Phase 41's separately-flagged opponent-animal-count correlation. No new, previously-uncatalogued archetype was found. **Our own team's PUBLIC trajectory (land=3, hands=11, crop mix, timing, animal count 6-7) is statistically indistinguishable across all 3 losses and 3 comparison wins (105757292, 105748242, 105746303)** — the shipped Submission K agent runs the same fixed, opponent-blind schedule regardless of matchup, so none of this sample's outcome variance traces to our own execution. A secondary, more novel but much less certain observation (n=6): all 3 losing opponents fully liquidated their crop tiles by day 29 like we do, while all 3 winning opponents left large amounts unliquidated (12-92 tiles) — a possible informal "opponent competence" proxy, flagged as suggestive only. **Recommendation: the 4th-quadrant land-cap lever (2 of 3 losses, and already flagged by Phase 41/43) is the more promising concrete next-phase target than animal count (1 of 3 losses)** — analysis only, no agent code changed. Full detail in `results/phase52/PHASE52_RECENT_LOSSES_ANALYSIS_REPORT.md`.
>
> **Update, post-Phase 51 — the HIRE pacer fails its own mandatory sanity check decisively (-88.9% on the 4-seed screen, 4/4 seeds regressed), so the 4th-quadrant TOMATO/GOOSE retest was correctly never reached.** `scripts/phase51/hire_paced_execution.py` extended Phase 36/45's cash-flow-adaptive pacer pattern to HIRE itself — the one purchase type BUY_LAND/BUY_ANIMAL/BUY_SEED all now pace but HIRE (confirmed directly, `agents/phase21/execution.py` lines 191-203, byte-identical in Submission K's own shipped `scripts/phase46/feed_priority_execution.py`) still spends its full cumulative Fibonacci hire cost against the FULL `money` balance with zero reserve — gating it instead against `money - land_purchase_reserve`, reusing the same posture-aware reserve concept BUY_LAND/BUY_ANIMAL/BUY_SEED already validated. Per this phase's own explicit stop-rule, the mandatory first step (run the new HIRE pacer on Submission K's own unmodified, capped-animal portfolio — `scripts/phase48/capped_animal_portfolio_agent.py`, no 4th quadrant, no new crop — on the 4-seed isolated screen) was run BEFORE touching the 4th-quadrant idea at all, and it failed hard: mean final money $70,419.75→$7,814.50, **every one of the 4 development seeds regressed**, idle-action fraction rising (0.095-0.116→0.155-0.195 across seeds). **A direct trace (seed 700002, `scripts/phase51/trace_sanity_regression.py`) confirms, rather than assumes, the mechanism**: gating HIRE against `money - land_purchase_reserve` collapses hand count to **0 hands for days 4-12** (vs. 11 hands already reached by day 10 in Submission K's own unmodified baseline), because HIRE's cheapest possible cost (`_fib(0)=1`) still can't clear the reserve floor whenever money sits below it (observed money $32-$77 on days 2-6, well under the $150 CLOSE-posture reserve) — and with zero hands there is no labor to generate the harvest/feed revenue that would ever let money climb back above the reserve, a self-reinforcing freeze. **This is exactly the same self-defeating feedback loop Phase 36 already diagnosed and explicitly warned against** when it deliberately chose NOT to gate HIRE with any shared reserve in the first place (Phase 36 report Section 3, quoted directly in that module's own docstring) — this phase's attempt is a fresh, honest re-confirmation of that same warning landing on HIRE directly, not a new failure mode. Per the brief's own instruction, the 4th-quadrant TOMATO/GOOSE dedicated-hands retest (Phase 33/46/48's repeatedly-blocked idea, whose diagnosed blocker IS a HIRE cash-timing problem) was correctly never built on top of a HIRE layer already shown to regress on its own. **Recommendation: do not pursue this specific HIRE-pacer design further** — any future attempt at fixing HIRE's own affordability check needs a fundamentally different gate than "hold back `land_purchase_reserve`," since that floor is routinely violated by ordinary early-game cash levels and HIRE (unlike BUY_ANIMAL/BUY_SEED) has no substitute mechanism to recover once frozen. Submission K remains the correct, unaffected upload candidate — nothing in the shipped strategy was touched. Full detail in `results/phase51/PHASE51_HIRE_PACER_REPORT.md`.
>
> **Update, post-Phase 50 — Submission K packaged and validated, ready to upload; no packaging-level discrepancy found anywhere, in either the shipped strategy or this phase's own new validation tooling.** Phase 48's Part C candidate (the capped-animal-target fix: `scripts/phase48/capped_animal_portfolio_agent.py::make_capped_animal_portfolio_agent`, confirmed directly to default `cap_total=8`, composing `scripts/phase48/capped_animal_portfolio.py::make_capped_animal_target_fn` — a proportional cap wrapping `agents/phase21/portfolio.py::portfolio_targets` unmodified, touching only the `"animals"` field of its output — with `scripts/phase46/feed_priority_execution.py::make_feed_priority_execution_agent`, byte-identical to Submission J's own shipped execution layer) was packaged following Phase 47's exact structure: a package-local `main.py` with the same Kaggle-compatible root-resolution fallback, 14 dependency files (one more than Phase 47's 13 — the two new Phase 48 wrapper files replace Phase 46's single adapter). Every existing `kaggriculture_*.tar.gz` in the repo root was listed before choosing a letter (per this project's own "confirm, don't assume" precedent) — A-J confirmed taken, **K confirmed free and used**. **Dependency-closure check clean** (all 14 non-`main.py` files byte-for-byte SHA-256-identical to their repo sources), **cold-process test clean** (`DONE`/`DONE`, $70,724.00 vs. random's $0.00 under `exec()`-based loading with `__file__` unset), **full regression suite unchanged at 128/133** (same 5 pre-existing stale-hash-pin failures held since Phase 7/20). **Packaged-artifact re-confirmation against the full 15-seed set and all three required opponents matched Phase 48's in-repo numbers exactly, to the dollar, on all 45 seed×opponent data points (zero mismatches)** — Submission G 15/15 (margin $14,842.73, recovering baseline's one remaining loss), Submission C 15/15 (margin $43,501.13), Jonaid 15/15 (margin $36,256.73), each up from Submission J's own 14/15, 15/15, 15/15. This phase's own new re-confirmation script was built directly against Phase 47's own template, with both of that phase's previously-found pitfalls (the `REPO_ROOT` dirname()-count off-by-one, the missing `importlib.invalidate_caches()` call) checked via explicit runtime assertions rather than assumed fixed by copying the pattern — both assertions passed cleanly, and unlike Phase 47, **no bug was found or needed fixing anywhere this phase**, honestly reported as a genuinely uneventful packaging run. **`kaggriculture_phase50_submission_K.tar.gz` (SHA-256 `22a48aec...796cb3c`) is ready to upload.** Full detail in `results/phase50/PHASE50_SUBMISSION_K_PACKAGING_REPORT.md`.

> **Update, post-Phase 49 — combining Phase 45's BUY_SEED pacer with Phase 46's FEED-priority fix produces a real, unexplained negative interaction; the CARROT/TOMATO cash-gated retest was correctly never reached.** Per its own explicit stop-rule, this phase halted at the mandatory sanity check: running both previously-validated, independently-safe execution-layer fixes together (on Submission I's unmodified portfolio, no new crop) scored -9.47% on the 4-seed isolated screen — worse than either fix alone (seed-pacer alone: -0.41%; FEED-priority alone, i.e. Submission J: +0.72%), and worse than BOTH individually on 3 of 4 seeds, not just the weaker one. A direct trace found neither fix's own diagnosed mechanism reappeared (land timing on schedule, animal-escape economics actually the best of all four candidates, idle-time not worse) — the cost is real but diffuse, the same character as Phase 45's own "diffuse cash competition" finding, not a single traceable collision. **Lesson generalized explicitly**: validating two execution-layer changes independently does not certify their combination, even when they target ostensibly orthogonal resources (cash reserve vs. worker-turn priority) — a genuinely useful process finding for any future phase wanting to stack validated fixes. Per the phase's own instruction, the cash-gated CARROT/TOMATO design was correctly never built on top of a layer already showing an unexplained regression. **Recommendation, reinforced**: Submission J (Phase 46's fix alone) remains the correct upload candidate — this phase found no reason to prefer any combined variant over it. The cash-gated CARROT/TOMATO idea itself remains neither confirmed nor refuted; a future attempt should run on top of a SINGLE validated fix (most naturally Phase 45's seed pacer alone, since that's the one whose own diagnosed collision the slice would otherwise re-trigger), not the combined layer. Full detail in `results/phase49/PHASE49_CASH_GATED_CARROT_TOMATO_REPORT.md`.
>
> **Update, post-Phase 48 — two of Phase 46's own recommended follow-ons (fertilizer, 4th-quadrant TOMATO/GOOSE) retested through its FEED-priority execution layer and closed negative again, for different, directly-traced reasons; a third question (the animal target's own achievable ceiling) produced a real, validated win.** Part A recomposed Phase 33's opportunistic (non-preempting, tier-4/5) fertilizer design on top of `scripts/phase46/feed_priority_execution.py` instead of the plain shape Phase 33 originally used (confirmed by direct read that Phase 33's own files predate both Phase 36's `BUY_ANIMAL` pacer and Phase 46's FEED tier-0.5 reorder). Result: **-98.6%** mean isolated money on the 4-seed screen — decisively WORSE than either Phase 30's naive attempt (-21.3%) or Phase 33's own opportunistic attempt (-26.9%), not rescued by the newer execution layer. Direct trace found fertilizer's turn-by-turn path-dependent perturbations (same mechanism Phase 33 diagnosed) interact worse, not better, with Phase 46's more finely cash-gated `BUY_ANIMAL` reserve threshold: hand-count stabilization delays from ~day 10 (no fertilizer) to ~day 21 (with it), leaving far too little runway for the cash ramp to recover. Correctly stopped at the screen per this project's standing rule — no full validation run. Part B recomposed Phase 33's dedicated-hands 4th-quadrant TOMATO/GOOSE design the same way: **-66.5%/-71.1%** on the 4-seed screen — a real improvement over Phase 33's original -92.3%/-94.1% but still decisively negative. **Direct trace confirms, rather than assumes, that Phase 33's diagnosed failure mode (the daily-recurring Fibonacci `HIRE` cost for dedicated hands, since hands reset to `[]` every day) is a fundamentally different mechanism from the one Phase 46 fixed** — `HIRE`'s code is byte-identical between the old and new execution layers, untouched by either the FEED/WATER tier reorder or the `BUY_ANIMAL` reserve gate; the SE quadrant is still bought on day 24, essentially as late as Phase 33's own traced day 26, still too late for TOMATO/GOOSE to produce any revenue. This is a cash-timing problem (HIRE), not the worker-turn-scheduling problem Phase 46 fixed, exactly the distinction the brief asked to check rather than assume. Part C asked a different, previously-open question: extending Phase 46's own `capacity_probe.py`-style instrumentation to track owned-animal count at the END of every day (not just day 29) found a genuine **steady-state equilibrium of 6-9 animals** (mean 8.00 isolated / 7.18 vs. Submission G, days 20-29) — cumulative purchases already reach 16-20 over the game (close to the 17 target), but roughly half subsequently escape, so **17 is not achievable under this execution layer's realistic play regardless of game length, a real lower ceiling, not a still-converging ramp.** A capacity-aware wrapper around `agents/phase21/portfolio.py::portfolio_targets` (imported unmodified) capping the animal target at 8 improved isolated economy on **all 4 individual development seeds** (+18.2% mean), and full 15-seed head-to-head validation against all three required opponents is a clean, strictly dominant win over Submission J's own shipped baseline: **vs. Submission G, 14/15→15/15 (margin +102%, recovering baseline's one remaining loss); vs. Submission C, 15/15 held (margin +22.6%); vs. Jonaid, 15/15 held (margin +30.4%)** — zero new losses anywhere in the sample. **Ready to package** (not packaged this phase, per this project's standing precedent). Full detail in `results/phase48/PHASE48_FERTILIZER_LAND_ANIMAL_TARGET_RETEST_REPORT.md`.

> **Update, post-Phase 47 — Submission J packaged and validated, ready to upload; no packaging-level discrepancy found in the shipped strategy, though two real bugs in this phase's own NEW validation tooling were caught and fixed before trusting any number.** Phase 46's recommended Part-B-alone candidate (FEED tier-0.5 reprioritization, `scripts/phase46/feed_priority_execution.py` composed with `scripts/phase46/feed_priority_portfolio_agent.py`, confirmed directly against that phase's own Section 4/6 recommendation and Changed Files list — Part B+C's throttled variant is deliberately NOT shipped) was packaged following Phase 37's exact structure: a package-local `main.py` with the same Kaggle-compatible root-resolution fallback, `agents/phase21/portfolio.py::portfolio_targets` imported unmodified. No new adapter file was needed — Phase 46's own `feed_priority_portfolio_agent.py` already mirrors Phase 37's adapter shape exactly, so it ships from exactly where it already lives, the same "package what was already validated, don't relocate it" precedent Phase 37 set for Phase 36's pacer. **Dependency-closure check clean** (all 13 non-`main.py` files byte-for-byte SHA-256-identical to their repo sources), **cold-process test clean** ($63,794.00 vs. random's $0.00, `DONE`/`DONE` under `exec()`-based loading with `__file__` unset), **full regression suite unchanged at 128/133** (same 5 pre-existing stale-hash-pin failures held since Phase 7). **Packaged-artifact re-confirmation against the full 15-seed set and all three required opponents matched Phase 46's in-repo numbers exactly, to the dollar, on all 45 seed×opponent data points (zero mismatches by direct programmatic diff)** — Submission G 14/15 (margin $7,345.47), Submission C 15/15 (margin $35,483.40), Jonaid 15/15 (margin $27,808.07), each up from Submission I's own 10/15, 15/15, 15/15. **Two real bugs were found and fixed, honestly disclosed, but both were in this phase's own brand-new cross-root validation script, not in any packaged strategy file**: an off-by-one `dirname()` count in `REPO_ROOT` resolution (two calls instead of three, since the script sits three directory levels below the repo root) silently pointed "the repo" at `scripts/` instead of the actual root, and a missing `importlib.invalidate_caches()` call after repointing `sys.path` let the `agents` regular (non-namespace) package's cached path resolution persist across the repoint — both diagnosed by direct standalone repro before being trusted, not assumed away. **`kaggriculture_phase47_submission_J.tar.gz` (SHA-256 `9b850ecd...c8b3643f`) is ready to upload.** Full detail in `results/phase47/PHASE47_SUBMISSION_J_PACKAGING_REPORT.md`.

> **Update, post-Phase 46 — the shared root cause behind four independent rejections (Phases 30/33/42/45) finally targeted directly, and it's a real, validated win.** Part A extended Phase 30's own idle-worker-turn capacity check (`scripts/phase46/capacity_probe.py`, a pure-measurement instrumented copy of Submission I's shipped execution logic) across two full 30-day trajectories at real scale (isolated seed 700000, vs. Submission G seed 701002), splitting FEED's shortfall into the two distinct failure modes the brief asked to separate: **assignment-race loss against WATER** (FEED loses the tier-1 nearest-worker comparison while workers ARE still free, 62.9%/65.9% of FEED-relevant turns) vs. **worker exhaustion** (tier 0 already claimed every worker before tier 1 was reached, 24.1%/27.2%). Race-loss dominates by ~2.5x, meaning the fix is a priority-ordering problem, not primarily a raw-capacity one — idle-action fraction (7.0%-9.3%) reconfirms Phase 30's original ~7% finding at this later phase's real animal/crop scale. Part B built `scripts/phase46/feed_priority_execution.py` (composing, not reimplementing, Phase 36's validated `BUY_ANIMAL` pacer) giving FEED a new tier strictly ahead of WATER. **An honestly-reported failed first attempt**: literally merging FEED into HARVEST's own tier 0 (the brief's literal suggestion) is a decisive regression (-89.5% isolated money, escapes UP not down 8.25→14.25, idle fraction roughly doubling) — the exact self-defeating-substitution failure mode Phase 36 already found once, because FEED then competes with time-sensitive harvests instead of just WATER. The tier-0.5 design (precedence over WATER only, never contesting HARVEST) is a real, non-regressing fix: 4-seed screen +0.72% money, escapes 8.25→7.50, no idle/crop-throughput regression. Part C (a further capacity-aware `BUY_ANIMAL` throttle, capping owned animals at 0.7x hand count) cut escapes further (8.25→6.25) at flat economy (-0.01%), trading a smaller final animal count (9.5→8.0) for reliability, per the brief's own framing. **Full 15-seed validation is a clean, decisive win for Part B alone**: vs. Submission G, 10/15→**14/15** (mean margin $4,031→$7,345, +82%), recovering 4 of baseline's 5 losses while introducing **zero new losses** anywhere in the sample; vs. Submission C, 15/15 held with margin +36.8% ($25,939→$35,483); vs. Jonaid, 15/15 held with margin +23.8% ($22,464→$27,808). Part B+C reaches the same 14/15 vs. G with an even larger margin but by fixing all 5 original losses while creating one new one elsewhere — a different, not strictly better, trade-off, so **Part B alone (no throttle) is the recommended, ready-to-package candidate** (packaging itself not done this phase, mirroring Phase 36's own precedent). **Retroactive retest, honestly reported as mixed, not spun positive**: re-running Phase 42's exact unmodified CARROT/TOMATO day-15 slice through the new FEED-priority execution layer made it WORSE, not better (-4.42% vs. Phase 42's own +0.08% wash, 3 of 4 seeds selling zero slice units) — that rejection's blocker is a `BUY_SEED` cash-flow collision (Phase 42/45), a different resource than the worker-turn scheduling this phase fixed, so fixing FEED does not rescue it. Phase 30's fertilizer (the other capacity-ceiling rejection) was not re-tested this phase but is now a concrete, well-motivated next-phase candidate. Full detail in `results/phase46/PHASE46_SCHEDULING_CAPACITY_REPORT.md`.

> **Update, post-Phase 45 — two independent probes, both closed negative, both honestly reported.** Part A extended Phase 36's `BUY_ANIMAL` cash-pacer pattern to `BUY_SEED` (`scripts/phase45/seed_paced_execution.py`, a per-crop-type reserve check reusing the same posture-aware `land_purchase_reserve` concept) — economically neutral on its own (-0.41% on a 4-seed screen) and, by direct trace, a REAL fix for the specific land-timing collision Phase 42 diagnosed (3rd land quadrant back to day 11, matching baseline, vs. Phase 42's traced 9-day delay to day 20). But re-testing Phase 42's day-0 CARROT/TOMATO slice through the new pacer still lost decisively (-25.4% mean vs. shipped, $59,144.50→$44,102.25) — a smaller, more diffuse cash-competition effect than Phase 42's found, but still a real net loss, so the full 15-seed run was correctly skipped per this project's own cheap-screen-first precedent. **Separately traced Phase 41's open question** (Submission I's final animal count sitting flat at 7-9 despite `_ANIMALS_RUNGS` targeting 17): confirmed by direct 2-seed trace that `BUY_ANIMAL`'s reserve is NOT too conservative (purchases track the target rung closely, 13 of 17 bought by day 21 in both seeds) — the real mechanism is **chronic escapes matching purchases** (8 of 13 escaped isolated, 13 of 13 escaped vs. Submission G), a worker-turn scheduling-capacity problem (FEED competing with HARVEST/WATER/PLANT/DIG for the same greedy nearest-worker assignment at real scale), the same category of architectural gap Phase 30 already found for fertilizer — reported as diagnosis-only, correctly not force-fixed. Part B built a wrapper around `agents/phase21/portfolio.py::portfolio_targets` (imported unchanged) that shifts a bounded 0.15 fraction of NEW/unplanted-tile allocation from STRAWBERRY/MELON toward WHEAT once the opponent's PUBLIC tile count in either crop (via the already-built `agents/phase3/opponent_observation.py`) hits 8 — never touching already-growing STICKY tiles. Validated on the full mandatory 15-seed set against all three required opponents: **every one got worse than Submission I's own shipped baseline** — Submission G, the closest matchup, flipped from a net win (10/15, +$4,031 mean margin) to a net loss (4/15, -$2,146 mean margin); Submission C fell 15/15→12/15 (+$25,939→+$19,088 margin); Jonaid fell 15/15→14/15 (+$22,464→+$21,754 margin). This corroborates, on fresh evidence and a broader opponent set, why `agents/phase21/portfolio.py`'s own dormant `OPPONENT_STRAWBERRY_DOMINANCE_THRESHOLD` mechanism was left unreachable after Phase 24 — STRAWBERRY doesn't actually crash much at these agents' real combined selling pace, so retreating from it trades away real revenue for glut protection the market mostly doesn't need at this scale. **Neither part is ready to package; Submission I remains the current best, unchanged.** Full detail in `results/phase45/PHASE45_SEED_PACER_AND_OPPONENT_STEERING_REPORT.md`.

> **Update, post-Phase 28 — diminishing returns confirmed honestly; ready to package.** Traced all 6 current losses fresh: three distinct patterns, not one shared cause (a one-off early-collapse outlier on seed 700000; four seeds showing the same residual endgame pattern Phase 27 already substantially narrowed but didn't fully close; one seed of ordinary game variance with no dominant mechanism). A real, separate bug WAS found — `agents/phase21/` never got Phase 16's wheat-reserve-for-feed fix, paying ~2.2x Submission G's feed costs — but every fix attempted (unconditional, cash-gated, day-and-cash-gated) made the full 15-seed result worse, not better, because reserving wheat destabilizes this agent's own cash timing unpredictably (unlike `agents/phase15/`, where the same fix was safe). **Reverted rather than forced through** — confirmed by exact, byte-for-byte reproduction of Phase 27's result after reverting. Full validation unchanged: **9/15 (60.0%) vs. Submission G, 14/15 (93.3%) vs. Submission C.** Recommendation, and the one being followed: package `agents/phase21/` now rather than continue chasing individual losses on this benchmark — two consecutive diagnose-fix cycles (13.3%→40.0%→60.0%) was a genuinely strong run, and this phase's honest finding is that the easy wins on this specific benchmark are exhausted.

> **Update, post-Phase 27 — 60% win rate against Submission G, first time crossing 50%.** Phase 26 correctly diagnosed the mechanism (forfeited final-day harvest revenue) but applied a global fixed-day fix that helped 3 seeds and broke a 4th, since the "right" liquidation day is per-game and per-tile variable. This phase replaced it with the actually-correct fix: per-crop planting cutoffs (`29 - first_yield_day`, derived directly from the engine's own HARVEST-blocking code: WHEAT/CARROT day 27, STRAWBERRY/MELON day 19) and per-tile digging logic (a standing crop is only cleared once ITS OWN remaining scheduled yields can no longer land by day 29, not on a shared calendar date). Result: the seed Phase 26 broke recovered clean (+$3,129, better than original), the seed it created was kept and widened, plus 2 more flipped and a 3rd came within $71. **Win rate 40.0%→60.0% (9/15), mean gap -7.6%→-2.2%.** Win rate vs. Submission C held at 14/15 with improved margin. The remaining 6 losses no longer look dominated by liquidation timing — one (701002) was already flagged in Phase 25 as losing under every crop ratio tested, a different, still-unaddressed factor. Recommended next step (not yet decided): a fresh diagnostic trace of the *current* remaining losses (not the ones already fixed), same discipline that's now worked twice in a row — versus considering this a strong enough candidate to package now, since it already beats the actual strongest live agent more often than not.

> **Update, post-Phase 26 — Reframe 2's hypothesis directly refuted; the real mechanism is endgame timing, not risk appetite.** Tracing the 5 closest losses to Submission G showed `agents/phase21/` was actually AHEAD for most of each game, only losing the lead in the final 1-2 days — not a spending-behavior gap. The risk-posture layer was still built and validated exactly as scoped (live opponent-money margin classifier, 2 cash-safety levers) — re-run on the same 5 seeds, it produced **byte-identical results**, directly confirming risk posture isn't the answer for these losses (not just inferred). **The real mechanism, confirmed by turn-level trace**: `agents/phase21/` fully liquidates all crop tiles by day 28, forfeiting the season's actual final day of harvest revenue — Submission G, still holding a few standing tiles into day 29, captures real cash on the last day that `agents/phase21/`'s fixed schedule already gave up. A direct fix (delaying the liquidation-start/dig days by 1) flipped one close loss to a win and narrowed two others substantially, but flipped a different, previously-marginal win into a loss elsewhere — net win rate unchanged at 6/15, though mean margin improved (-9.4%→-7.6%). **Root cause of the mixed result, diagnosed directly**: the "right" liquidation day is per-game and per-tile variable (depends on each tile's own harvest-cycle phase), not a fixed global day threshold — the fix moved two constants when what's actually needed is dynamic, per-tile timing computed from each crop's own `first_yield_day`/`interval`. **Next: a per-tile-aware liquidation redesign**, not more fixed-threshold tuning or a push further into Reframe 2's original risk-posture framing (directly falsified for these specific losses).

> **Update, post-Phase 25 — the crop-ratio lever is exhausted; a concrete, evidence-backed pivot to Reframe 2.** A systematic sweep (mirroring Phase 22's successful one) found no further improvement — worse, the 4-seed screening pass's apparent winner (0.70/0.70) performed WORSE on the full 15-seed set (3/15 vs. G, down from 6/15), a real small-sample-screening trap caught before it did damage. Win rate vs. Submission G stays at 6/15 (40.0%), Phase 24's ratios restored. **The informative finding**: one seed (700000) loses badly to G under every ratio tested — not a crop-economics problem. Looking at the full loss table, remaining losses skew close/moderate-margin while wins skew wide — exactly the shape Reframe 2 (win-probability objective) targets. **Confirmed directly from the engine source before committing to build it**: `obs0.farms` is the literal same shared list assigned to both players (`kaggriculture.py` lines 261, 271) — opponent money is fully public, every turn, no inference needed. No phase in this project has ever used live opponent cash margin as a decision input. Next: build Reframe 2 — a relative-score-aware risk-posture layer on top of Reframe 1's now-solid economics.

> **Update, post-Phase 24 — root cause found, real progress, gap not yet closed.** Neither of Phase 23's two hypotheses explained the losses — traced games showed no chronic cash collapse and no ramp-speed gap; both agents reach the same land/hand scale at the same pace. The real mechanism, found by reading the financial ledger: **STRAWBERRY never actually crashes at either agent's real mixed-portfolio selling pace** (realized ~$270-288/unit for both, essentially uncrashed) — Phase 21's Step 1 glut experiment only tested MELON under concentrated single-crop selling, and that conclusion was wrongly generalized to STRAWBERRY, which `agents/phase21/`'s WHEAT-heavy design then avoided for no real reason, leaving $30-33k/game on the table relative to G's STRAWBERRY-heavy approach. Reversing the day≥8 crop weighting (favor STRAWBERRY, keep MELON's confirmed opening-only role unchanged) took the win rate vs. Submission G from 2/15 (13.3%) to **6/15 (40.0%)**, mean gap from -17.5% to -9.4% — real, validated, but G still wins more (9-6). **Recommendation, and the one being followed: this is a crop-ratio calibration problem, the same category Phase 22 already solved once with a direct sweep — do a systematic STRAWBERRY/WHEAT ratio sweep next, before considering Reframe 2/3.** Nothing traced so far points at needing win-probability posture or market-as-weapon; the inferred day≥8 ratio (chosen from the trace, not swept) likely isn't optimal yet.

> **Update, post-Phase 23 — the real test, and a sobering but valuable correction.** Head-to-head against Submission G (not the synthetic benchmark) on the full 15-seed sample: `agents/phase21/` **loses decisively, 2-13 (13.3%)** — G ahead on mean (+17.5%), median, and consistency. The synthetic realistic-opponent benchmark's 15/15 result was real but measured the wrong thing: it has zero shared-market discipline or opponent-awareness of its own, so beating it never actually tested whether Reframe 1 competes with this project's real strongest work. (For context: `agents/phase21/` does beat Submission C decisively, 15-0 — it's a genuine improvement over the old lineage's baseline, just not yet over G.) **Two disclosed, untested hypotheses for the gap**: (1) execution/calibration maturity — G went through 4 full tuning phases (15/16/19/20) against real data; `agents/phase21/` has had 1 (Phase 22); (2) G's liquidity guard was purpose-traced against its own real collapse patterns (Phase 20); `agents/phase21/`'s cash-safety throttle was a quick addition never stress-tested the same way. **Recommendation, and the one being followed: do not promote `agents/phase21/`, and do not proceed to reframes 2/3 yet either** — building more architecture on an agent that loses to G would compound an unexamined gap. Next: a focused diagnostic phase tracing `agents/phase21/`'s actual losses against G, to determine whether the gap is closeable with more Reframe-1 execution polish or genuinely needs Reframe 2/3.

> **Update, post-Phase 22 — the strongest validated result in the project so far, on the full 15-seed sample.** A single sweep found Phase 21's original opening ratio (WHEAT 0.40/MELON 0.60) was an unexpected local minimum — both less WHEAT (0.35, the real benchmark's own ratio) and more (0.45, 0.50) beat it; only pushing much further (0.55) caused a genuine collapse. WHEAT 0.50/MELON 0.50 closed the isolated-economy gap from -39% to -6.6% relative to the realistic-opponent benchmark. On the full 15-seed development+validation+held-out set: **15/15 (100%) head-to-head against the realistic-opponent benchmark**, mean margin +196% (ours $46,930 vs. theirs $15,828), every single seed a clear win — confirming Phase 21's 4/4 result was real, not small-sample luck. `agents/phase21/` is still explicitly not submission-ready (never validated head-to-head against Submissions C/E/F/G directly, only against the synthetic benchmark; reframes 2 and 3 untouched) — but this is now a serious candidate, built on a fundamentally different and better-grounded architecture than the entire Phase 15-20 lineage.

> **Update, post-Phase 21 — Reframe 1 confirmed decisively through the real engine, and a fresh agent already beats a much more honest benchmark.** `results/phase21/PHASE21_SHARED_MARKET_PORTFOLIO_REPORT.md`: the shared-market glut hypothesis from `docs/FRESH_STRATEGY.md` was tested with two real competing sellers (not just the simulator) — a second MELON producer costs a MELON grower **66.5%** of final money; a second WHEAT producer costs a WHEAT grower only **12.4%**. Large, decisive, exactly as predicted from the engine's glut-curve constants. The realistic-opponent benchmark (built from Dmitry Larko's real 30-day trajectory, replacing Submission C/E as the validation bar) reaches $52,261 isolated — 2.4x C/E's own range. A first-cut portfolio controller built around the confirmed hypothesis (MELON time-boxed to the opening only, WHEAT as the durable backbone, opponent-aware STRAWBERRY sizing) **beats that benchmark 4/4 head-to-head**, despite a weaker isolated economy ($31,875 vs. the benchmark's $52,261 — the early WHEAT-heavy mix sacrifices cash-flow speed, a real, disclosed flaw). This is a new agent (`agents/phase21/`), entirely separate from the Phase 15/G lineage, built at only 4 seeds — not yet at this project's usual validation rigor. **Not yet submission-ready**, and reframes 2 (win-probability objective) and 3 (market-as-weapon) haven't been touched, per this phase's deliberately narrow scope.

> **Update, post-Phase 20 — Submission G ready, and a strategic pivot is underway.** `results/phase20/PHASE20_LIQUIDITY_GUARD_REPORT.md` built a liquidity guard for `agents/phase15/`, correctly diagnosed as structurally different from Submission C's F-005 case (2-3 recurring lump-expenditure collapses, not one early crisis — needed a per-turn re-armable throttle plus a pre-emptive land-purchase reserve check, not a one-shot guard). Result: head-to-head win rate recovered from Phase 19's disqualifying 53.3% to **86.7% (13/15)**, isolated money barely moved (mean $57,659→$56,665, still 14/15 clearing $50k). **Packaged and ready: `kaggriculture_phase20_submission_G.tar.gz`.**
>
> **Separately, and more importantly going forward**: after this, the project owner raised a fundamental critique — validating against Submission C/E was never a meaningful bar once we confirmed (via live Kaggle data pulled this session) how weak they are relative to the real ladder. Rather than continue iterating `agents/phase15/`'s architecture defensively, a fresh-from-first-principles re-read of the actual engine source (`vendor_kaggriculture/kaggriculture.py`) and rules produced `docs/FRESH_STRATEGY.md` — three reframes no phase has designed around: (1) the market is a **shared** inventory pool, and MELON's glut curve (quadratic, the harshest in the game) mechanically explains why real top players abandon it for WHEAT (flattest curve) at scale — something Phase 8's isolated-economics testing structurally could never see; (2) Kaggle's score is **win/loss only, margin doesn't count**, yet every agent so far (C through the Phase 15 lineage) was designed to maximize money with win-rate checked after, not designed around win-probability from the start; (3) since both players' sells hit the same shared inventory, market impact could in principle be wielded against an opponent, not just minimized for yourself — speculative, flagged as such, worth a cheap feasibility check only. **Next: a genuinely new-architecture phase built around these three ideas**, reusing only the verified infrastructure (market simulator, replay forensics, real ladder data) — not built on top of `agents/phase15/`.

> **Update, post-Phase 19 + live-game analysis — the money target is cleared, but F is missing safety infrastructure C/E already have, confirmed two independent ways.** Fresh top-ladder data (pulled directly from Kaggle's live API for current #1-adjacent players Dmitry Larko and Milan Leonard) showed F's targets were miscalibrated: real strong players settle at only 3 land quadrants (not 4) and ~11 hands (not 13), run a much larger 58-63 tile crop footprint (not ~50), and fully liquidate every crop tile by day 29 (a mechanic F never had at all). Phase 19 recalibrated to this data and it worked dramatically on economics — mean isolated money $43,735→$57,659, finally clearing the $50-80k bar (13/15 seeds vs. 2/15 before). **But head-to-head win rate against Submission C/E collapsed from 15/15 (100%) to 8/15 (53.3%)**, traced directly to repeated $0 cash crashes under real competitive pressure — a failure mode isolated "vs. pass" testing structurally can't expose. **This exact mechanism was independently confirmed from a completely different angle**: pulling Submission F's actual real Kaggle game history showed the same thing live — a real loss where F crashed to $0 by day 10 and never fully recovered (final $612 vs. opponent's $23,556), and a real win/loss record of only 2/6 (33%) against the live population, far below the 100% synthetic figure. **Root cause, confirmed both ways**: F is a from-scratch macro-controller (agents/phase15/) that never inherited Submission C/E's F-005 liquidity guard, sell-safety fix, or any of the defensive work from Phases 1-14 — it has zero cash-safety mechanism. Phase 19's recalibrated code is NOT promoted (win-rate regression disqualifies it) and the shipped Submission F package is unaffected. **Next, clear step: build an F-005-style liquidity guard for agents/phase15/ itself**, then re-validate both economics and head-to-head win rate before reconsidering promotion.

> **Update, post-Phase 18 — Submission F is built, validated, and ready to upload.** `kaggriculture_phase18_submission_F.tar.gz` (SHA-256 `16d3d0eb...854e38`, 15 files) packages the Phase 15/16 macro-controller agent. Two honest course-corrections along the way, both worth remembering: (1) Phase 17's wider 15-seed validation caught that Phase 16's "$326 short of the bar" reading was small-sample luck — the true isolated-money mean is ~$43,735, still short of the original $50-80k target; (2) given that, **the promotion decision was deliberately reframed around win-rate rather than isolated money** — Phase 17's same 15-seed validation shows a clean **15/15 (100%) head-to-head win rate against both live submissions, +79% mean margin**, which is the metric that actually determines Kaggle's Elo-style live rank (`docs/LEADERBOARD_DIAGNOSTIC.md` §1), not isolated production economics against opponents this agent never played. Phase 18 re-confirmed the packaged artifact's behavior matches Phase 17's numbers to the dollar, cold-process tested clean, and regression suite stayed at the same 128/133 baseline every phase since 7 has held. **Ready for manual upload to Kaggle** — the original $50-80k stretch target remains open for a future iteration if pursued further, but is no longer the gate on shipping this agent.

> **Update, post-Phase 15 — the thesis is validated, the target is not yet cleared.** `results/phase15/PHASE15_MACRO_CONTROLLER_REPORT.md`: the new macro-controller agent, built exactly per this plan (combined, proportional, early commitment to real-opponent-derived targets), **beats Submission C and E head-to-head in all 4/4 development seeds, +68% mean ($37,184 vs $22,104)**, and reaches a mean isolated $45,126 — a genuine ~1.5-1.6x step-change over C's best-ever ~$28-29k. **Not promoted**: it falls short of this plan's own $50-80k bar (only 1/4 seeds clears $50k, mean sits below all 4 real opponents' actual final banks). The single largest lever in the whole phase was a bug fix, not a design change — a "sticky assignment" bug was silently abandoning already-growing crops every time the day-indexed schedule shifted, costing ~197x on one seed alone ($214→$42,154) once fixed. Diagnosed remaining gap: target *selection* is confirmed correct (grounded directly in real opponent data, already within their observed ranges) — the shortfall is now believed to be **execution efficiency at this larger combined scale** (the task scheduler was only ever validated single-crop, no-animal, at smaller scale — Phase 10's "already near-optimal" finding doesn't necessarily hold at 13 hands/13 animals/50 tiles across multiple crop types simultaneously). The sell-timing layer measured as a wash (not helping, not clearly hurting) and is a candidate to cut or rework. **Next step: a narrowly-scoped follow-up targeting scheduling efficiency at scale, not a bigger rebuild** — see the roadmap for the Phase 16 prompt once sent.

> **Update, post-Phase 32 — fertilizer thread closed, honestly, on its second real attempt.** Both capacity-freeing mechanisms were tried: dedicated fertilizer hands (worse, -44.5% at best — Fibonacci hire costs for a hand whose fertilizer supply doesn't scale with labor outpace the payoff) and a reduced crop-tile ceiling to trade footprint for fertilizer room (closest at -10%: -8.5%, still a loss; -20% collapsed to -88%). Both increased fertilized-tile count meaningfully (peak 17-22 vs. Phase 30's 11.2) but never closed the gap, because **manufacturing the capacity costs more than fertilizer repays at this agent's scale** — a different, more specific failure mode than "not enough slack." Per the phase's own pre-registered rule, neither cleared the cheap 4-seed screen, so no full validation was run and no submission was warranted. **Submission H remains the best version, unchanged.**
>
> **Taking stock: three real "big lever" attempts this session (TOMATO/GOOSE, fertilizer ×2) have now all failed, each for a distinct, well-diagnosed reason** — not for lack of trying, and not because the ideas were bad on paper (all three were grounded in real engine mechanics and real top-ladder data). This is itself informative: `agents/phase21/`'s current WHEAT/STRAWBERRY/COW/SHEEP portfolio and capacity allocation looks like a genuine local optimum for this specific architecture, not an oversight. Two paths remain open, neither yet attempted: (1) targeted diagnosis of the two still-unexplained loss patterns from Phase 28 (seed 700000's chronic early-collapse outlier, and seed 701002's never-fully-diagnosed non-timing factor) — cheap, narrowly scoped, matches this project's most successful pattern (Phase 24, Phase 27); (2) the untried clone-detection idea from the forum research — a different *category* of improvement (behavioral, exploiting non-adaptive opponents) rather than another economic lever.

> **Update, post-Phase 30 — fertilizer's real-data premise confirmed, but blocked by a capacity ceiling, not a bad idea.** Verified directly: Larko/Milan Leonard do sustain 27-35 fertilized tiles for ~13-14 days, exactly as claimed. Implementation worked correctly (confirmed by trace, matching the dormant reference's own priority tiers) — but only reached 2-9 tiles at a time, because a pre-build capacity check found `agents/phase21/` already running at just **7.0% idle worker-turns** (vs. Phase 16's 15-23% on the older lineage) — essentially no slack left. Adding fertilizer tasks starved PLANT/DIG/BUILD often enough to shrink the whole crop/animal footprint the agent depends on: isolated economy -21.3%, win rate vs. Submission G **9/15 → 0/15**. Reverted in full, confirmed byte-identical to Phase 27/28. **This is a different failure mode than TOMATO/GOOSE (Phase 31) — not weak economics, a scheduling/capacity bottleneck**, and real top players clearly do find room for it, meaning it's solvable in principle. Next candidate, not yet sent: deliberately free capacity first (dedicate extra hands to fertilizer duty, or trade some crop-tile ceiling for the yield boost) before re-attempting, rather than bolting fertilizer onto an already-saturated schedule.

> **Update, post-Phase 31 — TOMATO and GOOSE/EGG tested, both a clean negative.** Both have real, engine-confirmed glut-resistance (TOMATO's `sqrt` curve, GOOSE/EGG's `log` curve matching WHEAT's) — GOOSE's resistance prediction held almost perfectly (contested vs. uncontested differ by only ~1%). But neither pays off: TOMATO's isolated economics are the weakest of every crop Phase 8 ever measured ($8.79/tile-day vs. STRAWBERRY's $30.62), and GOOSE's absolute revenue floor ($7,148 isolated) is so low that even zero competition never closes the gap to COW ($38,740). The mechanism: STRAWBERRY and COW — the incumbents these would replace — don't actually crash much under real two-player selling pace either (extending Phase 24's STRAWBERRY finding to a second and third resource: COW barely crashes, $32,916 contested). A resistant-but-weaker alternative has nothing to win by being resistant if the incumbent was never really crashing. Confirms the current WHEAT/STRAWBERRY/COW/SHEEP portfolio is already close to right. `agents/phase21/`'s shipped config (Submission H) untouched. **Fertilizer (Phase 30) remains the live, more promising lever — still pending.**

> **Update, post-Phase 29 — Submission H is live.** Packaged and validated with the same rigor as F/G (dependency closure, cold-process test, packaged-artifact re-confirmation matching in-repo numbers to the dollar), then uploaded. **This is the first agent in the project's history to clear a majority win rate against the previously-strongest submission** — 9/15 (60.0%) vs. Submission G, 14/15 (93.3%) vs. Submission C, both on the full 15-seed set. Built entirely on Reframe 1 (shared-market-durable portfolio) plus two real diagnose-fix cycles (crop economics, per-tile liquidation timing) — genuinely different architecture from the Phase 15-20 lineage it now beats. Live rating result pending (needs ~a day / 60-100 games to mean anything, per this project's own Elo-convergence findings). **Next, identified but not yet built: fertilizer.** Fresh top-ladder data (Larko, Milan Leonard — current #1/#2) shows both fertilize 32-35 tiles for 14 sustained days, roughly doubling yield on a third of their footprint — and it's nearly free (any animal makes 1 fertilizer/day available just for existing). Phase 6's original "fertilizer barely used, deprioritize" finding was drawn from a weaker, non-top-tier sample and is now confirmed wrong for the players who actually matter. A full, working fertilizer-application system already exists in `agents/phase2_3/common.py` (`collect_fertilizer`/`buy_fertilizer`/`fertilizer_apply` flags) but defaults to off and was never ported to `agents/phase21/`, which doesn't reference fertilizer at all.

> **Update, post-Phase 44 — a real, reusable "chase the current #1" forensics tool is built (`scripts/phase44/xray.py`), closing the gap between Phase 41's one-time real-opponent sample and an ongoing, refreshable one.** Everything downstream of "I have a team name and episode IDs" is fully scripted and idempotent (win/loss ledger, strategy fingerprint via Phase 41's own feature vocabulary, a new kinship/clone-detection metric, economy shape, and a deduplicating growing library). The one piece that stays manual: discovering who currently holds a given rank and their recent episode IDs — the live leaderboard is served by an internal, session-authenticated gRPC-web endpoint (`competitions.LeaderboardService/GetLeaderboard`, confirmed to return `403` unauthenticated) with no public unauthenticated equivalent, so this 2-3 minute lookup (the same one Phase 19/34/39/41 already did by hand) stays a small, documented manual step feeding a fully-automated pipeline. **Real demo against `keiz` (the ladder's current #1, having moved on from Phase 41's Crop Dusta)**: 5W-0L-0T, mean margin +$8,254.20, remarkably stable land=3/hands=10 scale, CARROT/TOMATO in 4/5 and 2/5 games — independently reconfirming Phase 41's 40% diversification finding on a fresh sample. The kinship detector correctly clustered all 5 of keiz's games with Phase 41's existing gold-tier rows (Crop Dusta, Jesse Bullard, Himanshu Kumar, Vishal Dhariwal) with zero false-positive matches to weak archetypes — a real, if informal, validation that the metric works. This tool is now standing infrastructure for keeping the opponent-population-diversity lead current as the real ladder shifts.
>
> **Update, post-Phase 43 — Submission I decisively BEATS a real, validated 4th-quadrant opponent (15/15, +$22,463.80/game). A third clean result pointing away from Submission I's own execution/scale as the real-vs-synthetic gap's cause.** Phase 34's original `sundar_archetype.py` reconstruction still fails its own isolation sanity check (re-confirmed, not a regression — Phase 34 already spent 3 rounds on it). A second, independent reconstruction built from real episode 105405216 (Jonaid — 4 quadrants, 24 animals, 5-crop diversity) passed its sanity check and lost to Submission I on all 15 seeds. **The striking part**: the real Jonaid actually beat Submission I in their real live game — but the reconstructed Jonaid, run through the exact same unmodified execution engine Submission I uses, loses badly. Holding execution constant like this isolates the variable to target schedule alone, and shows that blindly copying a real 4th-quadrant opponent's raw scale numbers underperforms Submission I's own tuned 3-quadrant targets — real evidence AGAINST pursuing a 4th-quadrant extension, not for it. Part B (building one) was correctly not attempted — no loss, no trigger. **Recommendation**: adopt the Jonaid archetype as a standing secondary/regression-monitoring benchmark alongside Submission G/C (not a promotion bar — Submission I beats it too decisively to discriminate real improvement from noise); flag `sundar_archetype.py` as known-broken so a future phase doesn't rediscover this. This is the third measurement phase in a row (39, 40, 43) to come back clean on Submission I's own side, further strengthening the case that the real-vs-synthetic gap is about opponent-population diversity on the real ladder, not anything wrong with Submission I itself.
>
> **Update, post-Phase-43-adjacent — live research into real public Kaggle notebooks/repos surfaced two things worth carrying forward.** (1) A rank-#52 player's own published meta-analysis notes "full three-quadrant expansion is now common; it is not itself an edge" at the real top tier — directly corroborating Phase 43's own finding the same day. (2) That same rank-#52 player's own tracked decided win rate, at rating ~2700+, is **49.7%** (88W-89L/177 real episodes) — a genuinely important reframe for this project's long-standing "why does real win rate sit near 50% even when synthetic testing predicts more" question: Elo matchmaking pairs you against comparably-rated opponents by construction, so a win rate hovering near 50% at any *sustained* rating band may simply be what the ladder structurally looks like, not evidence of a hidden weakness in Submission I specifically. Rating trajectory over time, not win rate in isolation, may be the more meaningful signal to track going forward. A real public repo (mohui666, rank #343, github.com/mohui666/kaggriculture-v66-agent) also confirmed a stage-aware, multi-route decision architecture keyed on public opponent state (including explicit clone/mirror detection) is a real, shipped, ranked approach — conceptually validating this project's own long-standing "opponent-aware differentiation" reframe (`docs/FRESH_STRATEGY.md`), just not yet built here.
>
> **Update, post-Phase 42 — CARROT/TOMATO diversification tested and closed, honestly negative, with the real blocker identified.** Phase 41's real-data lead (40% of real trajectories run a small CARROT/TOMATO slice) was built as a wholly additive variant on top of Submission I's unmodified portfolio (6 CARROT + 2 TOMATO tiles, sized against each crop's own glut-depth budget). The naive version (active day 0) was a severe loss (-37.4% on the 4-seed screen) — but [VERIFIED] by direct trace, NOT a market-glut problem (CARROT/TOMATO sold at 96-147% of base price whenever grown); the cause is a cash-flow collision: `BUY_SEED` has zero cash pacing (the same category of gap Phase 35/36 diagnosed and fixed for `BUY_ANIMAL` only), and the extra recurring seed spend delayed the 3rd land quadrant from day 11 to day 20 on the traced seed. Delaying the slice to day 15 removed the harm but also removed almost all utilization — a statistical wash (+0.08%), 4/4 head-to-head unchanged either way. Full 15-seed validation correctly skipped per this project's own "don't spend a full run on a cheap screen that already shows nothing" precedent (Phase 30/32/33). **Not packaged; Submission I unchanged.** Real conclusion: the idea itself isn't wrong, but it's blocked on an architectural gap — `BUY_SEED` needs the same cash-aware pacer Phase 36 built for `BUY_ANIMAL`, which is out of scope for a portfolio-only phase.
>
> **Update, post-Phase 41 — two concrete, untested real archetypes found; both Phase 39/40's execution-side dead ends are now behind us.** A fresh 16-episode/30-trajectory real sample (rank #1 down through Submission I's own live matchmaking band) surfaced five archetypes, two of which this project has never actually validated against in the form real players run them: (1) **CARROT/TOMATO as a minor diversification slice** (5-15 tiles) added on top of an otherwise MELON/STRAWBERRY/WHEAT-anchored portfolio — seen in 40% of sampled trajectories including gold-tier Crop Dusta and keiz; Phase 33 Part A only ever tested CARROT as a monocrop STRAWBERRY-replacement (correctly rejected — a different question). (2) **Whole-portfolio 4th-quadrant scale-up** — real players (Sundar, Jonaid, Alden Jenish S) take a 4th land quadrant as more of everything already being grown, not a dedicated side-crop; Phase 33 Part C only tested the latter and found an unrelated failure mode (daily hire-cost tax), and Phase 34's Sundar reconstruction — the one prior attempt at this shape — was built but never carried into an ongoing validation benchmark. **A suggestive but noisy loss-pattern observation** (n=9 real games, not causal): mean opponent animal count is 16.4 in Submission I's 5 losses vs. 7.0 in its 4 wins, with two exceptions breaking the pattern; also noted, Submission I's own final animal count sits flat at 7-9 across all 9 real games despite `agents/phase21/portfolio.py`'s target rungs reaching higher — an untraced open question for a future phase, not a finding. No agent/benchmark built this phase, correctly, per scope. **Recommended next, in order: (1) test the CARROT/TOMATO slice addition, (2) build and validate the whole-portfolio 4th-quadrant benchmark (reusing Phase 34's abandoned Sundar archetype), (3) a better-powered animal-count loss-pattern check with a synthetic control.**
>
> **Update, post-Phase 40 — the isolation-harness-artifact hypothesis is also REFUTED. Two measurement phases in a row have now ruled out execution-side explanations for the real-vs-synthetic gap.** Direct test: Submission I's shipped portfolio, re-run in an actual two-producer match against Submission G and separately against Submission C (not solo-vs-"pass"), on the same 4 development seeds and day≥11 matched-scale window Phase 39 used. Result: $/crop-tile/day under real competition (56.45-58.86) is essentially the same as Phase 39's isolated measurement (62.90) — if anything 6-11% lower, the opposite direction the hypothesis predicted. Tile idle-fraction and animal-servicing rate are flat across all three conditions too, so a real competitor doesn't change the execution layer's scheduling behavior. The small decrease is directionally consistent with ordinary shared-pool market pressure (the same mechanism Phase 21 Step 1 confirmed for MELON) but far too small (6-11%, not ~5x) to explain the Crop Dusta gap. **What this leaves standing**: with both candidate execution-side explanations (idle-time deficiency, isolation-harness artifact) now ruled out, Phase 39's other recommendation — that the real-vs-synthetic gap is more likely explained by opponent-population diversity in the real ladder than by anything wrong with Submission I's execution — is the strongest remaining lead. No fix was built; none was warranted.
>
> **Update, post-Phase 39 — the execution-efficiency hypothesis is REFUTED. This settles the "rebuild from scratch?" discussion.** At matched scale (3 land, 11-12 hands, day≥11 — real Crop Dusta episodes vs. Submission I's own isolated runs), Submission I is NOT less attentive than gold-tier play — it's measurably MORE attentive at the tile level (idle fraction 0.069 vs. Crop Dusta's 0.470, animal-servicing rate close at 0.707 vs. 0.757). The originally-hypothesized mechanism (a scheduling/idle-time deficit) does not exist and is closed. A real net-$/day gap IS visible in the raw numbers, but normalizing by active tile count ($/crop-tile/day) shows Submission I's range (49.9-81.6) brackets Crop Dusta's own balanced-mix game (62.3) almost exactly — only Crop Dusta's STRAWBERRY-dominant game is an outlier (298.6, ~5x every other data point), and n=2 real episodes can't distinguish "real gold-tier technique we haven't found" from "favorable seed" from "an isolation-harness artifact" (solo-vs-"pass" may not represent real two-producer STRAWBERRY economics — Phase 21 Step 1 already showed this matters a lot for this specific crop). **No fix was built — correctly, per this project's own discipline against speculative builds on an unclear diagnosis.**
>
> **What this means for "should we start a new architecture from scratch": no, not now — this result is the strongest evidence yet against it.** The architecture and execution layer are not the bottleneck (if anything they're already better than gold-tier at the thing measured). The repeating real-vs-synthetic gap (F: 33%/100%, H: 54.5%/60-100%, I: ~50%/67%-equiv) is now more plausibly explained by validation/benchmark limitations — this project has only ever tested against reconstructed single-trajectory benchmarks or its own old submissions, never a broad real population — than by anything a rewrite would fix. **Phase 39's own recommended next steps, not yet sent**: (1) test the isolation-harness-artifact hypothesis directly — rerun Submission I's late portfolio in a real two-producer match (vs. Submission G/C) and compare $/crop-tile/day to the solo-vs-"pass" numbers here; (2) grow the real gold-tier sample past n=2 before trusting the STRAWBERRY-outlier finding either way.

> **Update, post-Phase 38 — Reframe 3 (market-as-weapon) eliminated at the feasibility stage, cheaply, as designed.** Tested under PERFECT information (attacker knows exactly when the target will sell — the best possible case for this idea) across 3 conditions: simultaneous crash (a wash, within $1, exactly as the engine's unit-by-unit shared-pre-commit-inventory mechanic predicts by construction) and a pre-emptive 1-day-early sell (mechanistically the "correct" attack shape, and it was directionally better than simultaneous) — but the attacker lost money in every variant relative to its own natural-selling control (-$7,860 same-turn, -$5,233 pre-empt). **Root cause: abandoning continuous, market-tracking selling for a coordinated batch dump forfeits the price recovery natural selling rides, and that self-inflicted cost exceeds whatever damage the timed crash does to the target.** A real implementation would only make this worse (real sell timing must be inferred from incomplete public data — only `yield_units` is visible, not held shed inventory). All three of this project's founding reframes have now been tested: Reframe 1 (shared-market portfolio) — confirmed, shipped, working (Submissions H/I). Reframe 2 (win-probability/risk-posture) — built and validated, though the specific close-loss pattern it was aimed at turned out to be a liquidation-timing issue, not risk appetite (Phase 26-27). Reframe 3 (market-as-weapon) — cleanly eliminated. **Still awaiting: Phase 39 (matched-scale efficiency vs. Crop Dusta) — the one live, unresolved thread.**

> **Update, post-Phase 37 — Submission I packaged and validated, catching a real discrepancy along the way.** Step 1's direct-read check found Phase 36's fix was never actually merged into `agents/phase21/execution.py` — it only ever existed in the standalone `scripts/phase36/paced_execution.py`. Rather than package `agents/phase21/`'s unmodified (and therefore byte-identical-to-H) files, a new adapter wired the actual validated fix in correctly, per this phase's own constraint not to touch `agents/phase21/`'s own files. Cold-process test passed clean ($71,377 vs. random's $0), regression suite unchanged (128/133), and the packaged artifact's re-confirmed numbers matched Phase 36's in-repo validation exactly, to the dollar, on all 4 development seeds — no packaging fix needed. **`kaggriculture_phase37_submission_I.tar.gz` is ready to upload**, carrying the validated 10/15-vs-G / 15/15-vs-C improvement over Submission H.

> **Update, post-Phase 36 — a real, validated win: adaptive pacing beats Submission H.** Confirming Phase 35's diagnosis directly: the only purchase type with zero cash pacing was `BUY_ANIMAL` — exactly the mechanism traced as the front-loading culprit. Fixing only that (leaving targets, HIRE, and BUY_LAND untouched) gave a real improvement: isolated economy +6.0%, and on the full 15-seed set, **10/15 vs. Submission G (up from 9/15) and 15/15 vs. Submission C (up from 14/15)**. Worth remembering honestly: the first design (coupling the reserve to HIRE too) was actually worse (-66.2%) — it created a self-defeating loop where hands dropped to 0 exactly when animals needed feeding, causing more escapes. Decoupling it fixed that. **Separately: re-ran Phase 35's best SHEEP-heavy rebalance through the now-validated pacer — it improved (-29.2%→-21.4%, confirming pacing genuinely helps) but stayed a net loss.** This closes the Sundar-rebalance question properly: it was never purely a timing problem, `agents/phase21/`'s crop-heavy allocation is a genuine structural local optimum for this execution layer, even though real winners run a different ratio. **This phase's Stage 1 fix clears this project's submission bar and is ready to package — not yet done, packaging wasn't in this phase's scope.**

> **Update, post-Phase 35 — the real architectural gap, finally named directly.** Capacity-reallocation toward animals (Sundar-informed SHEEP-heavy, Crop Dusta-informed COW-heavy, with/without the 4th quadrant) failed every candidate, decisively — but NOT via the daily-hand-tax mechanism this phase was designed to dodge (idle capacity actually rose in every candidate, confirming worker-turns weren't the constraint). The real cause: **SHEEP ($500)/COW ($400) are large one-time capital purchases, and ramping the animal target on the same fixed day-0-11 schedule the current ratio uses front-loads that capital demand into the game's tightest cash window** — worsened by simultaneously cutting the crop revenue that funds it. Staggering the identical end-state to day 20 (A2) recovered most of the loss (best candidate, still a net loss) — direct confirmation of the diagnosis, not just a guess.
>
> **This reframes multiple earlier failures as one root cause, not three separate ones.** Fertilizer's dedicated-hands collapse, the TOMATO/GOOSE-on-new-land collapse, and this rebalance's collapse all trace back to the same thing: `agents/phase21/`'s targets are a **fixed, day-indexed calendar schedule** (`_LAND_RUNGS`, `_HANDS_RUNGS`, `_ANIMALS_RUNGS` — commit to X by day Y, regardless of actual cash state), with a reactive cash-safety throttle that can only brake AFTER the fact, never pace purchases to avoid the crunch in the first place. Real winners (Sundar, Crop Dusta) evidently pace capital purchases differently — dynamically, against actual cash flow, not a fixed calendar. **This is a real, named architectural gap, not yet addressed by anything tried this session** — a genuine next candidate for "aim big," bigger than another rung-table tweak: replace the day-indexed schedule with a cash-flow-adaptive pacer (buy the next increment when cash clears a safe multiple of its cost, every turn, not by a fixed day).

# The Big Swing: Why 15 Phases of Tuning Won't Reach Gold, and What Would

**Straight assessment, then a concrete plan for an architecture that can actually close the gap.**

## 1. The honest scorecard

Fifteen phases in, here's what's actually shipped and what it's worth:

| Submission | What it does | Measured improvement |
|---|---|---|
| D (Phase 7) | Fixes a dead sell-safety wire | Real, but only matters in the specific stuck-inventory case it targets |
| E (Phase 12) | Fixes a dead liquidity guard | **0/44 measured improvement** in every test run — the report itself says so |

And the research thread since (Phases 9-14) has been almost entirely **negative or neutral results**: don't raise the hand cap (Phase 9), scheduling wasn't the problem (Phase 10), animal capacity doesn't pay off in isolation (Phase 9/11), the one real live bug found doesn't matter at your actual scale (Phase 13/14). All of this was done rigorously and honestly — that's exactly why it's trustworthy — but rigor isn't the same as impact. **Every submission so far is defensive. None of them raise the ceiling.**

Meanwhile, the ceiling that actually matters hasn't moved:

- Your best recorded winning outcome, across every phase, is **~$28,000-29,000**.
- Community-reported RL agents on this same leaderboard are already at **$80,000-$160,000+** (per the forum research in `docs/LEADERBOARD_DIAGNOSTIC.md`).
- Real strong opponents parsed directly from your own replays (Phase 6) — Lai Eu Wen, moushun chen, Zach Locke — finish at **$76,000-$104,000+**, sustaining 8-13 hands and 10-14 animals *every single day*, on a portfolio Submission C's architecture has never once run.

That's a 3-5x gap. **No amount of adjusting thresholds inside the current architecture closes a 3-5x gap** — Phase 9/10/13/14 already proved this empirically: the current design has a real, measured ceiling around its current operating point, and pushing individual dials (hands, animals) past it makes things *worse*, not better, because the rest of the architecture wasn't built to use that scale.

## 2. Why tuning hit a wall — the actual mechanism, not a guess

This isn't mysterious. Read Phases 9-11 together and the story is complete:

- **Submission C's targets are small and fixed**: hands locked at 5 (`agents/phase3_5/response_policy.py`), animals capped at 6 even under maximum competitive pressure (`agents/phase3_8/animal_response.py`), crop choice defaults to MELON-solo (`agents/phase2_6/common.py`).
- **Phase 9 proved**: scaling *any one* of these up, alone, on the *existing* portfolio shape, loses money. More hands on a MELON-solo board is negative. More animals in isolation is negative or noise.
- **Phase 11 proved the missing piece**: scale the *whole portfolio* together — crops AND animals, in the proportions real strong opponents actually run — and labor's marginal value **reverses**, net +$7,690 instead of Phase 9's −$29,144, every hand count landing at 3-6x higher absolute money.

The conclusion Phase 9-11 together actually support, stated plainly: **Submission C isn't losing because any single number (hands, animals, MELON-vs-STRAWBERRY) is wrong. It's losing because it never commits to the combined scale real strong opponents operate at, all at once, early.** Planner v1 makes one conservative day-0 sizing decision and mostly stays there. Every phase since has been probing individual knobs on that same small, static commitment — which is exactly why each probe kept coming back "doesn't help" or "noise." You can't discover a step-change by wiggling one dial at a time on an architecture that was never designed to operate at step-change scale.

## 3. What the top of the field is actually doing (synthesized across everything gathered this session)

From real replay data (Phase 6, and this session's own live browser observation of the current #1 and #3):

- **Commitment happens fast and together.** Land, labor, and the first animal purchase land in the same 1-2 day window (Lai Eu Wen: day 9-10). Multiple hands are active from turn 1 of day 1, not ramped in gradually.
- **The commitment is broad, not deep in one crop.** By day 3, the winning side of a real top-5 head-to-head (Crop Dusta vs. Whyme Labs) already showed visibly denser, more advanced tile development than the losing side — tile-utilization density is what separated the win, observed directly in this session.
- **MELON is abandoned for STRAWBERRY by mid-game, in 4/4 real opponents studied** — even though Phase 8 proved STRAWBERRY doesn't win on raw isolated production economics. That contradiction is still unresolved (Phase 8 §6 flags it explicitly) and points at a two-player market dynamic this project has never directly tested — most likely, two players both running MELON (T=300, `above_target=3.60`, a hard crash penalty on oversupply) crash each other's MELON market far harder than an isolated single-player run ever shows, while STRAWBERRY's narrower depth matters less when only one side is committing to it at real volume.
- **Scale is sustained, not a one-time spike.** 13 hands and 12-14 animals held for 15+ consecutive days, each hand re-paid its full Fibonacci daily cost, because the production base (land + diverse portfolio) is big enough to make that scale worth its cost every day.

Submission C's architecture has never modeled any of this as a *combined, proportional* commitment — every layer that's been added (Variant D, the scaling response, the animal response) is a small, capped, reactive patch on top of a conservative baseline. That's the actual gap.

## 4. The big swing: what to build instead

Not another isolated no-submission probe. A genuinely new agent, built and validated with the same rigor this project has always used, but aimed at the scale the evidence now clearly supports — a **model-based macro controller**, exactly what Phase 6 §16 already recommended and explicitly justified (RL isn't viable yet: not enough credit-assignment data, only 2 real games on Submission C; a deterministic, evidence-grounded controller is both cheaper and more auditable given what's actually known).

**What it should do differently from Planner v1**, each point directly backed by a phase already run:

1. **Commit land + labor + a diverse crop-and-animal portfolio together, early** — not Planner v1's single conservative day-0 sizing pass. Target the moushun_chen-shaped portfolio Phase 11 already validated works (STRAWBERRY-anchored + COW/SHEEP), scaled toward real opponents' actual land/hand/animal levels (3-4 quadrants, 8-13 hands, 10-14 animals) rather than Submission C's current fixed 1-quadrant/5-hand/6-animal ceiling.
2. **Make hand/animal/land targets proportional to game state** (day, cash trajectory, opponent's observed scale — reusing the existing `agents/phase3/opponent_observation.py` telemetry this project already built) instead of Submission C's fixed thresholds. This is the structural fix for exactly what Phase 9 vs. Phase 11 revealed: a fixed target is either too small to pay off (Phase 9) or right for one portfolio shape and wrong for another (Phase 9 vs 11's contradiction) — a controller that scales its own targets with the actual game state doesn't have that failure mode by construction.
3. **Use the already-built, already-verified market simulator (`agents/phase4/market_model.py::simulate_sell`) for sell-timing**, not for portfolio/volume decisions (Phase 5 already proved that fails) — this was always the safer, recommended use (Phase 5 §16, Phase 8 §7) and is still unbuilt.
4. **Fix the fetch-bottleneck (Phase 13's finding) as part of this build**, since a controller operating at real animal scale (10-14, not Submission C's capped 6) will hit exactly the throughput ceiling Phase 13 found — irrelevant at C's current scale, directly relevant at the scale this new agent targets.
5. **Validate against real opponent replays as the primary benchmark**, not synthetic archetypes (which cap around 10 hands and have never represented the regime this controller targets) — reuse `agents/phase6/replay_forensics.py`, already built.

**Target, stated plainly so success is measurable**: this build should be judged against **$50,000-80,000+ final money on the moushun_chen-shaped benchmark**, not another few-thousand-dollar delta. If it can't clear that bar in controlled testing, it isn't ready to submit — same evidentiary discipline as every phase so far, just aimed at a number big enough to matter.

## 5. Sequencing

This is a genuinely bigger build than anything shipped so far — it deserves its own dedicated phase, not a quick prompt-and-go. Recommended shape:

1. **Design pass** (this document is step one) — confirm the target portfolio/scale numbers against a slightly larger real-replay sample than Phase 6's 4 opponents, if more are cheaply available (worth a quick pull before committing to exact targets).
2. **Build the controller** as a new, isolated adapter (following the project's own "new agents are separate experimental artifacts" convention) — reusing every already-validated piece (Phase 11's multi-resource execution logic, Phase 13's fetch fix, the market simulator, the opponent-observation telemetry) rather than rewriting anything that already works.
3. **Validate hard**: synthetic archetypes for basic sanity, but the real bar is performance against Phase 6's real-replay benchmarks and head-to-head against Submission C/E on fixed seeds.
4. **Promote only if it clears the $50-80k bar** — and if it doesn't, report why honestly, exactly as every phase so far has.
